# cucm root access
create a temporary root access on cucm OS platform &lt; 11.5

Depends on openssl for hmac() function.

This tool is able to create a working root password for the temporary user created with the command:

utils remote_account create
